// Local Imports
import { Record } from "@/models";

interface DecisionAnalyticsProps {
  records?: Record[];
}

export function DecisionAnalytics(_props: DecisionAnalyticsProps) {
  // Repository usage over time
  // Decision distribution across categories
  // Per date commits

  return (
    <section>
      <h2>Decision Analytics</h2>
      <p>Decision analytics are not available for this repository.</p>
    </section>
  );
}
